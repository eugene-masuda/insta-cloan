<% if @feed_items.any? %>
<div class="flex">
  <ol class="microposts">
    <%= render @feed_items %>
  </ol>
  <%= will_paginate @feed_items,
                    params: { controller: :static_pages, action: :home } %>
</div>
<% end %>

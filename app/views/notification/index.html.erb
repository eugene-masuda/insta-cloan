<% provide(:title, '通知') %>

<h1>通知</h1>

<% @notifications.each do |notification| %>
  <div class = "row notification_form">
    <%= notification_form(notification) %>
    <%= "( #{time_ago_in_words(notification.created_at)} )" %>
    </br>

  </div>
<% end %> 